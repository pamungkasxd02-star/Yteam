# `plugin`

OpenCode-compatible plugin bridge using portable subprocess JSON-RPC. Configure
`plugins.json` below `YTEAM_HOME` or set `YTEAM_PLUGIN_CONFIG`; plugin tools use
the same permission-aware agent registry as built-in and MCP tools.
